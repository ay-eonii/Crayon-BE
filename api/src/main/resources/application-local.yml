spring:
  datasource:
    url: jdbc:mysql://localhost:3306/crayon
    username: root
    password: dkdusl
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  application:
    kakao-client-id: safdsdf
    kakao-redirect-uri: https://localhost:8080
  data:
    mongodb:
      uri: mongodb://${HOST:localhost}:${PORT:27017}/${DATABASE:crayon}
      host: ${HOST:localhost}
      port: ${PORT:27017}
      database: ${DATABASE:crayon}
      username: ${USERNAME:admin}
      password: ${PASSWORD}
      authentication-database: admin
      uuid-representation: standard
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASS:}
      database: 0
      queues:
        database: 1
  main:
    allow-bean-definition-overriding: true

cloud:
  aws:
    credentials:
      accessKey: sdfasdf
      secretKey: asdfasdfasdf
    region:
      static: ap-northeast-2
    stack:
      auto: false
    route53:
      hostedZoneId: asdfasdf
    accountId: asdfasdf
    acm: ${ACM}

vite:
  uri: aasdfasdf
